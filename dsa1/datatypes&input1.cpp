#include<iostream>
#include<math.h>//for math functions
#include<string>//for string functions
#include<bits/stdc++.h>//for all standard libraries
using namespace std;//::std replacement
int main() {
    //note character has single qoutes'',& string has double quotes""
    char n='a' ;
    cout<<(int)n<<endl; 
    char m='9' ;
   //int  a= 97 ;
    cout<<int(m)<<endl;//write now here 9 has ASCII value 57
   //cout<<int(a)<<endl;//write now here 97 is dritly prinitng
    /*
    int a,b,c;
    cout<<"Enter every number by giving space"<<endl;
    cin>>a>>b>>c;//input 3 numbers in case of space only as it ignores the space
    cout<<" the value ares "<<a<<" "<<b<<" "<<c<<endl;
    //data types int, float, boolean, long,long long double etc etc every data type has range
    //string and getline
    */
    /*
    string a;
    cout<<" Enter string "<<endl;
    cin>>a;
    cout<<a<<endl;
    */
    //in case we required more string or we want two string then we have to redeclared it as many time we want it
    //as it does not accept space
    /*
    string ab,ba;
    cout<<" Enetr a1 and b1 "<<endl;
    cin>>ab>>ba;
    cout<<ab<<" is String1 "<<endl<<ba<<" is String2 "<<endl;
    */
    //now there is shortcut which is used to pick whole line untilline break
    /*
    string line;
    getline(cin,line);
    cout<<line<<endl;
    */
    

    return 0;
    
}