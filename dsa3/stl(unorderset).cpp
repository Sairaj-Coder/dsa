#include<bits/stdc++.h>
using namespace std;
//everything is similar  to set but in random order
//unique element but not sorted
void explain(){
    unordered_set<int> st;
    //particular order it has better complexity
    //lower bound upper bound functions are same
    //time complexcity is Big-O(1)
}